import React from 'react';
import CLoader from './cloader-component';

/**
 * Container Component
 *  - Write down all component logic here
 */
const Loader = (props) => {
    return <CLoader {...props} />
}

export default Loader